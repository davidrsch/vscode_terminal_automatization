name: Deploy Extension

on:
  push:
    tags:
      - "v[0-9]*.[0-9]*.[0-9]*"
  pull_request:
    branches:
      - "main"

jobs:
  build-vsix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Format check
        run: npm run format

      - name: Test
        run: npm test

      - name: Verify tag matches package.json version (tags only)
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          PKG_VERSION=$(node -p "require('./package.json').version")
          TAG=${GITHUB_REF#refs/tags/v}
          if [ "v$PKG_VERSION" != "v$TAG" ]; then
            echo "Tag $TAG does not match package.json version $PKG_VERSION"
            exit 1
          fi

      - name: Package VSIX artifact
        run: npm run package

      - name: Rename VSIX with release name
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          for f in *.vsix; do
            new_name="terminal-automatization-${GITHUB_REF#refs/tags/}.vsix"
            mv "$f" "$new_name"
            echo "Renamed to $new_name"
          done

      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: vsix
          path: |
            *.vsix

  publish:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') && github.repository_owner == 'davidrsch'
    needs:
      - build-vsix
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: vsix
          path: .

      - name: Publish to VS Code Marketplace
        run: npx @vscode/vsce publish --packagePath *.vsix --skip-duplicate
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}

      - name: Publish to Open VSX Registry
        run: npx ovsx publish *.vsix --skip-duplicate
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "terminal-automatization ${{ github.ref_name }}"
          files: "*.vsix"
          generate_release_notes: true
